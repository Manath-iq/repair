# Дизайн-система

Всё, что нужно, чтобы сверстать страницу, не выбирая цвета и размеры заново.

**Два правила, оба проверяются автоматически** (взяты из `grand-house-template`):

- ни одного хекса вне файла токенов;
- ни одной текстовой строки в компонентах — слова приходят из конфига.

```bash
grep -rnE "#[0-9a-fA-F]{3,8}" src/components src/styles/*.css   # должно быть пусто
```

---

## 1. Палитра

Направление: **тёплый светлый премиум**. Полотно не белое, а тёплое офф-вайт; белый — только карточки поверх него. Контраст полотна и карточки и есть вся глубина: теней почти нет.

Соотношение примерно **75 / 15 / 10** — светлое, тёмные пятна, акцент. Акцент только на действии и на ключевой цифре.

### Светлая тема

| Токен | Значение | Роль |
|---|---|---|
| `--canvas` | `#F3F1ED` | полотно страницы |
| `--surface` | `#FFFFFF` | карточки и панели |
| `--surface-sunk` | `#EAE7E1` | поля ввода, вложенные плашки, неактивные чипы |
| `--ink` | `#14141A` | заголовки и основной текст |
| `--ink-2` | `#55555E` | вторичный текст, описания |
| `--ink-3` | `#666670` | подписи 12–13px, мета-строки |
| `--line` | `#E4E0D9` | волосяные разделители |
| `--line-strong` | `#D3CEC5` | границы карточек, активные обводки |
| `--accent` | `#B4622F` | заливки, графика, линии выносок, активные состояния |
| `--accent-ink` | `#9A5127` | **акцентный текст любого кегля** |
| `--accent-soft` | `#F2E4DA` | подложка акцентной плашки |

### Тёмная секция и футер

| Токен | Значение | Роль |
|---|---|---|
| `--dark-bg` | `#101014` | фон тёмной секции, футера, карточки заявки |
| `--dark-surface` | `#1A1A20` | плашки внутри тёмного |
| `--dark-ink` | `#F2F0EC` | текст на тёмном |
| `--dark-ink-2` | `#A0A0AA` | подписи на тёмном |
| `--dark-line` | `rgba(255,255,255,.14)` | разделители и линии выносок на тёмном |
| `--dark-accent` | `#D98B4A` | акцент на тёмном (осветлён, иначе не читается) |

### Проверенный контраст

Все пары считаны, не на глаз:

| Пара | Отношение | Годно для |
|---|---|---|
| `--ink` на `--canvas` | **16,3 : 1** | всё |
| `--ink` на `--surface` | **18,4 : 1** | всё |
| `--ink-2` на `--canvas` | **6,5 : 1** | всё |
| `--ink-3` на `--canvas` | **5,0 : 1** | всё, включая 12px |
| `--accent-ink` на `--canvas` | **5,1 : 1** | всё |
| `--accent-ink` на `--surface` | **5,8 : 1** | всё |
| `--accent` на `--surface` | **4,44 : 1** | **не проходит 4,5** — только графика и текст 18px+ |
| `--dark-ink` на `--dark-bg` | **16,7 : 1** | всё |
| `--dark-ink-2` на `--dark-bg` | **7,3 : 1** | всё |
| `--dark-accent` на `--dark-bg` | **7,0 : 1** | всё |

**Правило:** акцентный текст — всегда `--accent-ink`. `--accent` не дотягивает до 4,5 : 1 и идёт
только на заливки, линии, иконки и крупный текст от 18px (для него порог 3 : 1). Если в макете
появился `--accent` на подписи 13px — это ошибка, а не вкусовщина.

---

## 2. Типографика

Три роли, три гарнитуры. Все три есть на Fontsource, все три с кириллицей, все самохостятся.

| Роль | Гарнитура | Где |
|---|---|---|
| Дисплейная | **Geologica** 500–600 | h1, h2, h3, цифры-герои |
| Основная | **Onest** 400–500 | весь текст, кнопки, меню |
| Данные | **Martian Mono** 400–500 | площади, сроки, миллиметры, доли, суммы, eyebrow, выноски |

**Моноширинный для цифр — не украшение.** `62 м² · 62 дня · от 613 000 ₽`, набранное моноширинным, читается как факт; набранное гротеском — как маркетинг. В нише, где решает прозрачность цены, это работает напрямую.

### Шкала

| Роль | Размер | Начертание | Интерлиньяж | Трекинг |
|---|---|---|---|---|
| Display XL — h1 | `clamp(40px, 6.2vw, 88px)` | Geologica 600 | 0.98 | −0.03em |
| Display L — h2 | `clamp(30px, 4.2vw, 56px)` | Geologica 600 | 1.02 | −0.025em |
| Heading M — h3 | `clamp(21px, 2vw, 28px)` | Geologica 600 | 1.15 | −0.015em |
| Heading S | `18px` | Geologica 500 | 1.3 | −0.01em |
| Body L | `18px` | Onest 400 | 1.55 | 0 |
| Body | `16px` | Onest 400 | 1.6 | 0 |
| Caption | `13px` | Onest 400 | 1.45 | 0 |
| Eyebrow | `11px` | Martian Mono 500 | 1 | 0.1em, UPPERCASE |
| Data | `14px` | Martian Mono 400 | 1.4 | 0.02em |
| Data XL — цифра-герой | `clamp(40px, 5vw, 72px)` | Martian Mono 500 | 1 | −0.02em |
| Callout — выноска | `11px` | Martian Mono 500 | 1.2 | 0.04em |

**Длина строки** — не больше 68 символов в абзацах, не больше 22 в заголовках. Заголовки переносятся вручную через `<br>` или `text-wrap: balance`, а не как получится.

### Приёмы из референсов

- **Одна часть заголовка — акцентом.** Ровно один такой приём на заголовок, не два. Реализуется `<span>` с `--accent-ink`.
- **Цифра-водяной знак.** `Data XL` размером 180–260px, `color: --line`, `z-index` под текстом. Максимум два раза на всю страницу.
- **Eyebrow над каждой содержательной секцией**, с номером: `— 03 · ЧТО ВНУТРИ`.

### Шрифты: технические грабли

Из `stroika-template`, повторяются один в один:

- Субсеты режутся скриптом на сборке. Берём кириллицу и латиницу; для Onest и Martian Mono **дополнительно latin-ext**.
- **Знак рубля U+20BD лежит в latin-ext.** Без него цифры цены рисуются нашим шрифтом, а `₽` — системным, и это видно невооружённым глазом. На странице с ценами в двадцати местах это провал.
- У Geologica latin-ext подключать не нужно: вторым номером в её стек ставится Onest, оттуда рубль и подтягивается.
- `font-display: swap`, вариативные версии, предзагрузка только тех начертаний, что есть на первом экране.

---

## 3. Сетка и раскладка

| Параметр | Значение |
|---|---|
| Контейнер | `max-width: 1240px` |
| Поля | 20px мобильный · 32px планшет · 48px десктоп |
| Колонки | 12, `gap: 24px` |
| Вертикальный ритм секции | `padding-block: clamp(80px, 9vw, 160px)` |
| Отступ заголовка от контента | `clamp(32px, 4vw, 64px)` |
| Базовая единица | 4px, все отступы кратны |

**Брейкпоинты:** `640` · `900` · `1200`. Три штуки, больше не заводить.

### Радиусы

| Что | Радиус |
|---|---|
| Крупная панель, hero-карточка, футер | `32px` |
| Фотография внутри панели | `24px` |
| Вложенная плашка, ячейка бенто | `20px` |
| Поле ввода | `16px` |
| Всё, что нажимается в один клик: кнопки, чипы, шапка, бейджи | `999px` |

### Тени

Референсы почти бестеневые — разделение идёт белым на офф-вайте. Тени только две:

```css
--shadow-rest:  0 1px 2px rgba(20,20,26,.04);
--shadow-hover: 0 14px 36px -14px rgba(20,20,26,.16);
```

Тень покоя — на карточках. Тень наведения — только на кликабельных карточках и только при `:hover`.

### Линии

Волосяные, `1px solid var(--line)`. На тёмном — `var(--dark-line)`. Никаких `2px` границ, никаких пунктиров, кроме анимации прочерчивания выносок.

---

## 4. Мотив выносок

Графическая подпись всего проекта. Приём из Dronea, Porsche, БПЛА и карточек материалов AURA — в нише ремонта не встречается.

### Устройство

```
                              ┌─ полка 28–40px, горизонтальная
                              │
   ●───────────────────────────  01  стяжка 60 мм, армированная
   │                             └─ номер + подпись
   └─ точка привязки 4px
```

| Элемент | Спецификация |
|---|---|
| Точка привязки | круг `4px`, заливка `--accent` (на тёмном — `--dark-accent`) |
| Линия | `1px`, `--line-strong` на светлом / `--dark-line` на тёмном |
| Полка | горизонтальный отрезок 28–40px перед подписью |
| Номер | `Callout`, `--accent-ink`, две цифры с ведущим нулём |
| Подпись | `Callout`, `--ink-2` на светлом / `--dark-ink-2` на тёмном |
| Максимум на одном изображении | **4 выноски.** Пять — уже схема, а не акцент |

### Правила расстановки

1. Линии **только горизонтальные или под 45°**. Кривых и дуг нет.
2. Две выноски не пересекаются. Если пересеклись — точка привязки выбрана неудачно, двигать её, а не линию.
3. Подписи выравниваются по общей вертикали слева или справа, а не каждая по-своему.
4. Подпись — **факт с единицей измерения**. `стяжка 60 мм` — да. `качественная стяжка` — нет.
5. Точка привязки ставится на то, о чём подпись, а не «куда влезло».

### Анимация

- Линия прочерчивается через `stroke-dasharray` / `stroke-dashoffset`, **480 мс**, кривая `cubic-bezier(.16,1,.3,1)`.
- Подпись проявляется `opacity` **240 мс** с задержкой 180 мс от начала линии.
- Стаггер между выносками — **120 мс**.
- Запуск: в hero — через 600 мс после загрузки; в остальных местах — по входу в экран.

### На мобильном выносок нет

Линии на ширине 375px превращаются в кашу. Вместо них — **список моноширинных строк под изображением**, с теми же номерами и подписями:

```
01   стяжка 60 мм, армированная
02   звукоизоляция 50 мм
03   4 линии электрики, медь 3×2,5
04   штукатурка по маякам, ≤2 мм на 2 м
```

Порог переключения — `900px`.

---

## 5. Компоненты

Для каждого — четыре состояния: покой, наведение, фокус, нажатие. Фокус виден **всегда**: `outline: 2px solid var(--accent); outline-offset: 2px`.

### Пилюля-кнопка (основная)

Чёрная заливка `--ink`, текст `--canvas`, радиус 999, высота 52px (мобильная — 56), паддинг 24px, внутри справа — белый круг 32px со стрелкой (приём Dronea).

- **наведение:** круг со стрелкой сдвигается на 3px вправо, фон светлеет до `#22222A`, 220 мс;
- **нажатие:** `scale(.985)`;
- **отключена:** `--surface-sunk`, текст `--ink-3`, курсор обычный.

### Пилюля-кнопка (вторичная)

Прозрачная, граница `1px --line-strong`, текст `--ink`. При наведении — фон `--surface`.

### Шапка-капсула

Белая с `backdrop-filter: blur(20px)`, фон `rgba(255,255,255,.72)`, граница `1px --line`, радиус 999, высота 60px, отступ сверху 16px. При скролле вниз > 40px — высота 52px и `--shadow-hover`, переход 240 мс.

### Фото-карточка

Радиус 24–32px, `overflow: hidden`. Фотография `object-fit: cover`. Единая обработка **на уровне CSS, не в фотошопе**:

```css
filter: saturate(.92) contrast(1.04);
```

При наведении на кликабельную карточку — `transform: scale(1.02)` у изображения, 700 мс, и `--shadow-hover` у карточки.

### Карточка-цифра

Плашка `--surface`, радиус 20px, паддинг 28px. Внутри сверху `Data XL` цифра `--ink`, под ней `Heading S` подпись, под ней `Caption` расшифровка `--ink-3`. **Расшифровка обязательна** — без неё цифра не работает.

### Ячейка бенто

`--surface`, радиус 20px. Высоты в ряду **намеренно разные**: соотношение 1 : 1,4 или 1 : 0,7. Ровных рядов из одинаковых ячеек в макете быть не должно.

### Чип-фильтр

Высота 40px, радиус 999, граница `1px --line-strong`, текст `--ink-2`.
Активный: заливка `--ink`, текст `--canvas`, границы нет.
Наведение неактивного: фон `--surface`.

### Слайдер площади

Дорожка 4px `--surface-sunk`, заполненная часть `--accent`, бегунок круг 24px белый с границей `--line-strong` и `--shadow-rest`. Значение — рядом, `Data`, редактируется вводом с клавиатуры тоже.

### Строка сметы

Три части в ряд: название `Body` `--ink-2` · доля `Data` `--ink-3` · сумма `Data` `--ink`, прижата вправо. Между строками — `1px --line`. При изменении входных данных сумма меняется **без анимации счётчика**: цифра, которая крутится, читается как реклама.

### Аккордеон

Заголовок `Heading S`, справа плюс 16px, превращающийся в минус поворотом одной палочки. Не стрелка. Раскрытие — `grid-template-rows: 0fr → 1fr`, 320 мс.

### Слайдер «до / после»

Вертикальный разделитель 2px белый, ручка — круг 40px с двумя стрелками. Тянется мышью и пальцем. Подписи `до` / `после` — моноширинные плашки в верхних углах.

### Мобильная липкая панель

Высота 64px + `env(safe-area-inset-bottom)`, фон `rgba(243,241,237,.86)` с `blur(16px)`, верхняя граница `1px --line`. Две кнопки в ряд с `gap: 8px`.

---

## 6. Движение

**Одна кривая на всю страницу:** `cubic-bezier(.16, 1, .3, 1)`. Пружинная, без отскока.

| Что | Свойства | Длительность | Стаггер |
|---|---|---|---|
| Появление блока при входе в экран | `opacity 0→1`, `translateY 20px→0`, `filter: blur(6px)→0` | 620 мс | 70 мс |
| Прочерчивание выноски | `stroke-dashoffset` | 480 мс | 120 мс |
| Слой на плане квартиры | `opacity`, `translateY 8px` | 520 мс | по входу карточки этапа |
| Слой разреза в тёмной секции | `opacity`, `translateY 12px` | 480 мс | 90 мс |
| Наведение на кнопку | `background`, `transform` | 220 мс | — |
| Наведение на фото | `transform: scale` | 700 мс | — |
| Раскрытие аккордеона | `grid-template-rows` | 320 мс | — |
| Смена шага в форме | `opacity`, `translateX 12px` | 260 мс | — |

### Что не анимируется никогда

- цифры — не «накручиваются»;
- секции не выезжают сбоку;
- параллакса фона нет;
- автопрокрутки нет ни у одной ленты, кроме бесконечной строки ЖК;
- ничего не двигается на скролле «просто так» — только три signature-элемента.

### `prefers-reduced-motion: reduce`

- выноски отрисованы сразу, без прочерчивания;
- план квартиры показан полностью собранным;
- слои разреза показаны все;
- бегущая строка ЖК останавливается и превращается в статичный ряд;
- появления блоков заменяются мгновенным показом.

Реализация всех трёх signature-элементов — **`IntersectionObserver` плюс CSS-переходы. Ни GSAP, ни Lenis.** На эту страницу библиотека не даёт ничего, чего не даёт наблюдатель, а весит больше всего остального скрипта вместе взятого.

---

## 7. Чеклист «не как у всех»

Пройти по готовому макету перед показом. Каждый пункт — то, чем страница отличается от сайтов конкурентов.

- [ ] Полотно тёплое, не белое и не серое.
- [ ] Hero — карточка с полями, а не фон, прибитый к краям экрана.
- [ ] Радиусы крупные: 32px у панелей, 999px у всего кликабельного.
- [ ] Ни одной ровной сетки 3×2 из одинаковых карточек.
- [ ] Под каждой цифрой есть строка-расшифровка.
- [ ] Все числовые данные набраны моноширинным.
- [ ] Выноски есть минимум в двух местах и подписаны фактами с единицами.
- [ ] Ровно одна тёмная секция в середине страницы.
- [ ] Ни одной иконки из бесплатного набора там, где можно поставить чертёж или фото.
- [ ] Ни одной градиентной кнопки, ни одной тени-свечения.
- [ ] Ни одного восклицательного знака и ни одной скидки.
- [ ] Все интерактивные элементы имеют видимый фокус с клавиатуры.
- [ ] Проверено на 375px: выноски свернулись в списки, ленты листаются пальцем, липкая панель не перекрывает кнопку формы.
